function symb(input) {
    let name = input[0];
    for (let i = 0; i < name.length; i++) {
        console.log(name[i]);
        // console.log(name.charAt(i)); same thing as above
    }
}
symb(["softuni"])