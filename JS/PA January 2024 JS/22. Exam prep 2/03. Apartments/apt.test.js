import { expect } from 'chai';
import { findNewApartment } from './findApartment.js';

describe('First test', function () {
    it('default pass', () => {
        expect(1).to.equal(1);
    });
    it('default pass', () => {
        expect(1).to.equal(1);
    });
});