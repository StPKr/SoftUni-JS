function solve(a, b, c) {
    let average = (a.length + b.length + c.length) / 3;
    console.log(a.length + b.length + c.length);
    console.log(Math.floor(average));
}
solve('chocolate', 'ice cream', 'cake');